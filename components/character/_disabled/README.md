# 🚫 Arquivos de Modelo 3D - DESABILITADOS

Estes arquivos foram temporariamente removidos do site para fazer um reset limpo.

## Arquivos nesta pasta:
- `Character3D.tsx` - Componente de renderização do modelo GLB
- `CharacterCanvas.tsx` - Canvas Three.js com iluminação e controles

## Para reativar:
1. Mova os arquivos de volta para `components/character/`
2. No `app/page.tsx`, importe e use o `CharacterCanvas`
3. Certifique-se de que o arquivo `/public/models/alon.glb` existe

## Arquivos relacionados (ainda no projeto):
- `lib/three/lipSyncController.ts` - Controle de lip sync
- `lib/three/modelLoader.ts` - Carregador de modelos GLB
- `types/character.ts` - Types do TypeScript

## Data de desativação:
2026-01-30

---

**Status:** Coluna central agora mostra apenas "3D Character Space" (placeholder vazio)
